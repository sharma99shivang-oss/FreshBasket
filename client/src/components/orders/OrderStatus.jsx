const steps = ['Pending', 'Confirmed', 'Packed', 'Shipped', 'Delivered'];
export function StatusBadge({ status }) { return <span className={`rounded-full px-2.5 py-1 text-xs font-bold ${status === 'Cancelled' ? 'bg-red-100 text-red-700' : status === 'Delivered' ? 'bg-brand-50 text-brand-700' : 'bg-amber-100 text-amber-800'}`}>{status}</span>; }
export function OrderTimeline({ status }) { const current = steps.indexOf(status); if (status === 'Cancelled') return <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm font-medium text-red-700">This order was cancelled.</p>; return <div className="mt-5 flex items-start justify-between gap-1">{steps.map((step, index) => <div key={step} className="flex min-w-0 flex-1 flex-col items-center gap-1 text-center"><span className={`h-3 w-3 rounded-full ${index <= current ? 'bg-brand-600' : 'bg-stone-200'}`} /><span className={`text-[10px] font-semibold sm:text-xs ${index <= current ? 'text-brand-700' : 'text-stone-400'}`}>{step}</span></div>)}</div>; }
