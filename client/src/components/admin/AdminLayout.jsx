import { ClipboardList, Image, LayoutDashboard, Package, Tags, Ticket, Users } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';

const links = [{ to: '/admin', label: 'Dashboard', icon: LayoutDashboard, end: true }, { to: '/admin/products', label: 'Products', icon: Package }, { to: '/admin/categories', label: 'Categories', icon: Tags }, { to: '/admin/inventory', label: 'Inventory', icon: Package }, { to: '/admin/orders', label: 'Orders', icon: ClipboardList }, { to: '/admin/users', label: 'Users', icon: Users }, { to: '/admin/coupons', label: 'Coupons', icon: Ticket }, { to: '/admin/banners', label: 'Banners', icon: Image }];
export default function AdminLayout() { return <div className="mx-auto flex max-w-7xl gap-4"><aside className="w-16 shrink-0 rounded-2xl bg-brand-700 p-2 text-white sm:w-52"><div className="mb-5 hidden items-center gap-2 px-3 pt-3 font-bold sm:flex"><LayoutDashboard size={19} /> Admin</div><nav className="space-y-1">{links.map(({ to, label, icon: Icon, end }) => <NavLink end={end} key={to} to={to} className={({ isActive }) => `flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold ${isActive ? 'bg-white text-brand-700' : 'hover:bg-brand-600'}`}><Icon size={19} /><span className="hidden sm:inline">{label}</span></NavLink>)}</nav></aside><section className="min-w-0 flex-1"><Outlet /></section></div>; }
