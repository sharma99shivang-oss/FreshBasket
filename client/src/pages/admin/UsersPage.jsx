import { Search, Users } from 'lucide-react'; import { useEffect, useState } from 'react'; import { toast } from 'react-hot-toast'; import { getUsers } from '../../services/adminService';
export default function UsersPage() { const [users, setUsers] = useState([]); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); useEffect(() => { const timer = setTimeout(() => { setLoading(true); getUsers({ search: search || undefined }).then(setUsers).catch(() => toast.error('Could not load users')).finally(() => setLoading(false)); }, 250); return () => clearTimeout(timer); }, [search]); return <section><h1 className="text-2xl font-bold">Users</h1><div className="relative mt-5 max-w-xl"><Search size={17} className="absolute left-3 top-3 text-stone-400" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search users" className="w-full rounded-xl border border-stone-300 py-2.5 pl-9 pr-3" /></div><div className="mt-5 overflow-x-auto rounded-2xl bg-white ring-1 ring-stone-200"><table className="w-full min-w-150 text-left text-sm"><thead className="bg-stone-50 text-stone-500"><tr><th className="p-4">User</th><th className="p-4">Phone</th><th className="p-4">Role</th><th className="p-4">Joined</th></tr></thead><tbody>{loading ? <tr><td colSpan="4" className="p-5"><div className="h-5 animate-pulse rounded bg-stone-200" /></td></tr> : users.map((user) => <tr key={user._id} className="border-t"><td className="p-4"><b>{user.name}</b><span className="block text-xs text-stone-500">{user.email}</span></td><td className="p-4">{user.phone}</td><td className="p-4"><span className="rounded bg-stone-100 px-2 py-1 text-xs font-bold">{user.role}</span></td><td className="p-4">{new Date(user.createdAt).toLocaleDateString('en-IN')}</td></tr>)}{!loading && !users.length && <tr><td colSpan="4" className="p-8 text-center text-stone-500"><Users className="mx-auto" />No users found.</td></tr>}</tbody></table></div></section>; }
