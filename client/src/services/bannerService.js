import api from './api';
export const getBanners = (params) => api.get('/banners', { params }).then((r) => r.data.data); export const createBanner = (data) => api.post('/banners', data, { headers: { 'Content-Type': 'multipart/form-data' } }).then((r) => r.data.data); export const updateBanner = (id, data) => api.put(`/banners/${id}`, data, { headers: { 'Content-Type': 'multipart/form-data' } }).then((r) => r.data.data); export const deleteBanner = (id) => api.delete(`/banners/${id}`); export const reorderBanners = (items) => api.patch('/banners/reorder', { items }).then((r) => r.data.data);
